# Grafana Alloy - copy to .env and set all required values
# Everything is controlled via env (see config.alloy). Used by docker-compose and Railway.

# --- Required: Grafana Cloud Metrics (Prometheus) ---
GRAFANA_PROMETHEUS_URL=https://prometheus-prod-24-prod-eu-west-2.grafana.net/api/prom/push
GRAFANA_PROMETHEUS_USERNAME=123456
GRAFANA_CLOUD_TOKEN=

# --- Required: Backend scrape target (host:port, must expose /metrics) ---
BACKEND_ADDRESS=backend_service_name:3000

# --- Required: Grafana Cloud Logs (Loki) ---
GRAFANA_LOKI_URL=https://logs-prod-XXX.grafana.net/loki/api/v1/push
GRAFANA_LOKI_USERNAME=123456

# --- Optional: Ports (defaults shown) ---
# Alloy UI port (local/docker-compose). On Railway, PORT is set automatically.
ALLOY_PORT=12345
# Loki push API port (for services sending logs to Alloy)
LOKI_SOURCE_PORT=3500
